# MongoDB connection (required)
MONGODB_URI=mongodb://localhost:27017/pdf_manager

# Directory where PDF files are stored (relative to backend or absolute path)
PDF_STORE_PATH=./data/pdf-store

# Server port
PORT=4000

# Optional: base URL for the API (e.g. https://api.example.com) when building download links
# API_BASE_URL=
